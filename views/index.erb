<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Gallery</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'media'
        }
    </script>
</head>
<body class="font-sans m-0 p-5 bg-gray-100 dark:bg-gray-900">
    <h1 class="text-center text-gray-800 dark:text-gray-200 mb-8 text-3xl font-bold">Photo Gallery</h1>
    
    <% if @photos.empty? %>
        <div class="text-center text-gray-600 dark:text-gray-400 italic mt-12">No photos found in db/photos</div>
    <% else %>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5 max-w-6xl mx-auto">
                <% @photos.each do |photo_path| %>
                    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden transition-transform duration-200 hover:-translate-y-1 hover:shadow-xl">
                        <img src="/photo/<%= ERB::Util.url_encode(photo_path) %>" alt="<%= ERB::Util.html_escape(File.basename(photo_path)) %>" class="w-full h-64 object-cover block">
                        <div class="p-4">
                            <div class="font-bold text-gray-800 dark:text-gray-200 mb-1 break-words">
                                <a href="/details/<%= ERB::Util.url_encode(photo_path) %>" class="text-gray-800 dark:text-gray-200 no-underline transition-colors duration-200 hover:text-blue-600 dark:hover:text-blue-400 hover:underline">
                                    <%= ERB::Util.html_escape(File.basename(photo_path)) %>
                                </a>
                            </div>
                            <div class="text-sm text-gray-600 dark:text-gray-400 break-words"><%= ERB::Util.html_escape(photo_path) %></div>
                        </div>
                    </div>
                <% end %>
        </div>
    <% end %>
</body>
</html>
